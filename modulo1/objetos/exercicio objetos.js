     //EXERCICIO DE INTERPRETAÇÃO DE CODIGO 

     ///Exercicio 1 -(a) R= será impresso o objeto [0] matheus nachterhaele
      // e o objeto [-1] virginia cavendish
      // objeto [2] canal- globo e o horario que irá passar o filme (14h)

      //Exercicio 2
      // (a)
      //R= será impresso o objeto nome, idade, e raça do cachorro, gato e tartaruga
      // será usando a função replaceAll para alterar o "a" pelo "o"
      // (b) R= atraves dela conseguimos realizar uma copia de um objeto
      //ou array inteiro 
      
      //Exercicio 3
      //(a)R= apenas a resposta "false" pois no console.log ele pergunta se 
      // a pessoa é backender e anteriormente já avisa colocado como "false"

    // (b)R= aparece underfined poisno console.log o programa pede a altura do usuario
     // não foi foi definida nenhum valor para altura do usuario. 
      
     // Exercicio de escrita de codigo
    
     const pessoa = { 
      nome = "Julia",
      apelidos = ["ju" ,"julinha","jujuba"]
       
     }
    
     console.log=(`Eu sou ${pessoa.nome}, mas pode me chamar de ${pessoa.apelidos[0]}, ${pessoa.apelidos[1]}, ${pessoa.apelidos[2].})